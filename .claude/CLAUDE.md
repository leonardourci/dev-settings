@RTK.md

# Rules

- Never git commit anything unless explicitly asked.
- When making git commits, commit as the user. Do NOT add `Co-Authored-By: Claude` trailer or any Claude attribution in commit messages.
- At end of every response that references files, list them in this format:
  - {filename.ext} - {full/absolute/path}
